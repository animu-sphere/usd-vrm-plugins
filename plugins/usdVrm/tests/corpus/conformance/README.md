# Conformance models

Single-feature VRM spec models that isolate one capability each (humanoid,
expression, look-at, mtoon, spring-bone, constraint), so a failure points at one
subsystem. VRoid-derived corpus alone does not exercise these edges (see
CORPUS.md "Coverage vs the policy axes").
