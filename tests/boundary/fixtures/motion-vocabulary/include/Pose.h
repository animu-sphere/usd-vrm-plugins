// SPDX-License-Identifier: Apache-2.0
//
// A header that is about to leave, as the check sees one. The comment may say
// VRM and humanoid freely; only the code below is scanned.
#pragma once

struct HumanoidPose
{
};

// The one name no ledger below disposes of, except the one that lists it.
struct VrmBindingHint
{
};

bool GetRequiredBones();
