# Hand-authored (not generated by `ost ci generate`).
#
# Documentation consistency: asserts the contract values the docs restate from
# the manifests, and that every local link resolves. Deliberately cheap — no
# runtime, no `ost`, no OpenUSD — so it can gate every PR in seconds.
#
# It does not generate documentation. The docs are hand-written; this only
# catches the facts that rot silently when the workspace changes shape.
name: docs-check

on:
  pull_request:
    paths:
      - "**/*.md"
      - "**/openstrata.plugin.yaml"
      - "**/openstrata.library.yaml"
      - "scripts/check_docs.py"
      - ".github/workflows/docs-check.yml"
      # The three files the mocopi rig-agreement check reads. Without them the
      # lane would pass by never running: a table edited on its own is exactly
      # the drift that check exists to catch.
      - "adapters/liveCapture/mocopi/src/SkeletonMap.cpp"
      - "adapters/liveCapture/mocopi/include/vrmAdapterMocopi/SkeletonMap.h"
      - "profiles/motion/*.yaml"
      - "libs/motionBvh/tests/corpus/recorded/redistributable/*.bvh"
  push:
    branches: [main]
    paths:
      - "**/*.md"
      - "**/openstrata.plugin.yaml"
      - "**/openstrata.library.yaml"
      - "scripts/check_docs.py"
      - ".github/workflows/docs-check.yml"
      # The three files the mocopi rig-agreement check reads. Without them the
      # lane would pass by never running: a table edited on its own is exactly
      # the drift that check exists to catch.
      - "adapters/liveCapture/mocopi/src/SkeletonMap.cpp"
      - "adapters/liveCapture/mocopi/include/vrmAdapterMocopi/SkeletonMap.h"
      - "profiles/motion/*.yaml"
      - "libs/motionBvh/tests/corpus/recorded/redistributable/*.bvh"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docs:
    name: docs consistency
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Check docs against the manifests
        run: python scripts/check_docs.py
